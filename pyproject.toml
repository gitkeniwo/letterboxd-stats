[project]
name = "letterboxd-stats"
version = "0.1.0"
description = "Letterboxd data visualization with Streamlit"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40.0",
    "pandas>=2.0.0",
    "altair>=5.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
init-db = "db.init_db:main"
